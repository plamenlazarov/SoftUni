var calcCylinderVol = function(arr) {
    var radius = arr[0],
        height = arr[1],
        volume = Math.PI * ((radius * 2) * height);

        console.log(Math.round(volume * 1000) / 1000);
}

calcCylinderVol([2, 4]);
calcCylinderVol([5, 8]);
calcCylinderVol([12, 3]);
